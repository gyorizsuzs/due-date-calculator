// BASE LOGIC

// MAIN FUNCTION
// CalculateDueDate(submitDate, TurnaroundTime, estimatedSolutionDate)

// DATA
// workingHours = start(9), end(17)
// workingDays = weekDay(+1), weekEnd(+3)

// calcEstimate = start + TAT

// sameDay OR !sameDay
// friday OR !friday
// isWorkDay + isWorkHour = isWorkTime
